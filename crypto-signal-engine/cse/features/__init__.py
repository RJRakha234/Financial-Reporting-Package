"""Feature layer: technical, volatility, flow, and statistical indicators.

Import submodules directly (``from cse.features import trend``) rather than
re-exporting here — ``cse.features.pipeline`` imports the sibling modules, so
eager re-exports would create an import cycle.
"""
