pdfplumber>=0.11
PyMuPDF>=1.24
reportlab>=4.0   # only needed for make_sample.py
pytest>=7.0      # only needed to run the tests

# Optional — only for `fincheck compare` when a scanned page has no text layer
# and must be OCR'd. Uncomment to enable, and also install the tesseract engine
# itself (not a pip package): `apt-get install tesseract-ocr` / `brew install
# tesseract`. A scanned PDF that already has a text layer needs none of this.
# pytesseract>=0.3
# Pillow>=10.0
