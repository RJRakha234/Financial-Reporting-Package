pdfplumber>=0.11
openpyxl>=3.1    # writing the Excel (.xlsx) output
PyMuPDF>=1.24    # only needed for the PDF-highlighting checker
reportlab>=4.0   # only needed for make_sample.py
pytest>=7.0      # only needed to run the tests
