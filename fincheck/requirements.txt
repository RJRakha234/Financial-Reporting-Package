pdfplumber>=0.11
PyMuPDF>=1.24
openpyxl>=3.1    # only needed for the Excel -> CSV converter (fincheck.xlsx2csv)
reportlab>=4.0   # only needed for make_sample.py
pytest>=7.0      # only needed to run the tests
